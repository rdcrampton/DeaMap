# In dev mode, API calls use Vite's proxy (same-origin, no CORS).
# The proxy is configured in vite.config.ts to forward /api/* to deamap.es.
# Leave VITE_API_BASE_URL empty so HttpClient uses relative URLs.
VITE_API_BASE_URL=
